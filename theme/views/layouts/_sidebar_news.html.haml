- $categories = get_terms( 'year_news', array( 'order' => 'DESC' ) )

%ul
  - $i = 0
  - foreach ( $categories as $cat )
    - #todo: better if clause.
    - if ( ($i == 0 && is_page('news')) || is_tax('year_news', $cat->slug) )
      - $class = 'active'
    - else
      - $class = 'inactive'
    %li{:class => "#{$class}"}
      %h2= link_to($cat->name, get_term_link($cat->slug, 'year_news'))
      - $i++
  %li.fill= "&nbsp;"